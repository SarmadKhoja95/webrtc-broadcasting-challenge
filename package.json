{
  "name": "signaling-server",
  "version": "1.0.0",
  "description": "Signaling server for webRTC code challenge",
  "main": "server.js",
  "scripts": {
    "build": "ng build",
    "heroku-postbuild": "ng build --prod",
    "start": "node server",
    "dev": "nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sarmad Khoja",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "socket.io": "^4.6.1"
  },
  "engines": {
    "node": "16.x"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
